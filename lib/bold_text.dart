import 'package:flutter/cupertino.dart';

class BoldText extends StatelessWidget {

  BoldText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
