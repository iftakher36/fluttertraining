import 'package:flutter/cupertino.dart';

class BoldText extends StatelessWidget {
  final double fontSize;
  final String family;

  const BoldText({Key? key, required this.fontSize, this.family = 'Roboto'})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

